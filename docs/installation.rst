============
Installation
============

At the command line::

    $ easy_install somepython

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv somepython
    $ pip install somepython
